# MF-SLAM 扑翼飞行器改进说明与移植指南

> 基线：`/home/dl/MF-SLAM-main`（长短焦 SLAM 原始版）
> 改进：`/home/dl/MF-SLAM`（针对扑翼飞行器 FWMAV 的改进版）
> 本文档记录所有改进点、在改进版中的具体位置，以及可移植到其它工程的通用思路。

改进的核心痛点：**扑翼飞行器机身高频抖动 + 远距离目标模糊 + 双目视差退化**。

---

## 一、初始化阶段改进（重点）

### 1.1 设计思想

原始 ORB-SLAM 双目初始化的门槛非常简单粗暴：

```cpp
// 原始 MF-SLAM-main/src/Tracking.cc
if(mCurrentFrame.N > 500) {
    // 直接开始建图
}
```

只看"提取到的特征点总数"，不管这些点**有没有有效深度**、**空间分布是否均匀**。在扑翼场景下，抖动 + 模糊会导致：
- 特征点数量够（>500），但真正三角化成功、有深度的点很少 → 初始地图稀疏、尺度不准；
- 特征点全部堆在图像某一块区域 → 几何约束退化，初始位姿病态。

改进思路：**用"质量"而非"数量"作为初始化门槛**。新增一个独立的质量评估函数 `CheckInitializationQuality()`，从三个维度综合判断，任一不达标就跳过本帧、不做初始化。

### 1.2 三个判定维度

| 维度 | 变量 | 默认阈值 | 含义 |
|------|------|----------|------|
| A. 特征总量 | `minFeatures` | 最大特征数 × 0.3（约 300） | 基础门槛，防止空场景 |
| B. 有效 3D 点 | `minEffective3D` | 100 | **真正有深度的点**，这是双目初始化的核心 |
| C. 网格覆盖率 | `minGridCoverage` | 0.2 | 10×10 网格中被有效点覆盖的比例，保证几何分布均匀性 |

三个条件 **AND 关系**，必须同时满足：`return condA && condB && condC;`

### 1.3 具体位置

- 质量评估函数：[Tracking.cc](file:///home/dl/MF-SLAM/src/Tracking.cc#L1575-L1652) `Tracking::CheckInitializationQuality()`
- 初始化入口调用：[Tracking.cc](file:///home/dl/MF-SLAM/src/Tracking.cc#L1459-L1573) `Tracking::StereoInitialization()`（第 1462 行由 `CheckInitializationQuality` 替换原来的 `N > 500`）
- 函数声明：[Tracking.h](file:///home/dl/MF-SLAM/include/Tracking.h#L123) `bool CheckInitializationQuality(int &nValidDepthPoints, int &nOccupiedCells);`
- 依赖的接口：[ORBextractor.h](file:///home/dl/MF-SLAM/include/ORBextractor.h#L71-L74) 新增 `GetMaxFeatures()`（返回 `nfeatures`），用于自适应计算 `minFeatures`。

### 1.4 核心代码（可直接移植）

```cpp
bool Tracking::CheckInitializationQuality(int &nValidDepthPoints, int &nOccupiedCells)
{
    // 1. 参数配置（可外提到配置文件）
    const int minFeatures = mpORBextractorLeft->GetLevels() > 0 ?
                            mpORBextractorLeft->GetMaxFeatures() * 0.3 : 300;
    const int minEffective3D = 100;      // 至少多少个有深度的3D点
    const float minGridCoverage = 0.2;   // 网格覆盖率下限

    nValidDepthPoints = 0;

    // 2. 网格配置（10x10）
    const int nGridCols = 10, nGridRows = 10;
    vector<bool> vGridOccupancy(nGridCols * nGridRows, false);

    // [关键修复] 网格尺寸必须减去图像有效区偏移 mnMinX/mnMinY，并用浮点避免精度丢失
    float cellW = (mCurrentFrame.mnMaxX - mCurrentFrame.mnMinX) / (float)nGridCols;
    float cellH = (mCurrentFrame.mnMaxY - mCurrentFrame.mnMinY) / (float)nGridRows;

    // [兼容] 新双目模型(mpCamera2)只遍历左目 Nleft；旧模型遍历 N
    int totalFeatures = mpCamera2 ? mCurrentFrame.Nleft : mCurrentFrame.N;

    // 3. 统计有效深度点 + 网格覆盖
    for(int i = 0; i < totalFeatures; i++) {
        bool hasDepth = false;
        if (mpCamera2) {
            // 新双目：靠左右匹配索引判断
            if (mCurrentFrame.mvLeftToRightMatch[i] != -1) hasDepth = true;
        } else {
            // 旧双目/RGBD：直接判断深度值
            if (mCurrentFrame.mvDepth[i] > 0) hasDepth = true;
        }
        if(!hasDepth) continue;

        nValidDepthPoints++;

        const cv::KeyPoint &kp = mCurrentFrame.mvKeys[i];
        // [关键修复] 坐标同样减去偏移量
        int gridX = static_cast<int>((kp.pt.x - mCurrentFrame.mnMinX) / cellW);
        int gridY = static_cast<int>((kp.pt.y - mCurrentFrame.mnMinY) / cellH);
        // [关键修复] 严格 clamp，防止边缘极值点数组越界导致段错误
        gridX = std::max(0, std::min(gridX, nGridCols - 1));
        gridY = std::max(0, std::min(gridY, nGridRows - 1));

        vGridOccupancy[gridY * nGridCols + gridX] = true;
    }

    // 4. 覆盖率
    nOccupiedCells = 0;
    for(bool occupied : vGridOccupancy) if(occupied) nOccupiedCells++;
    float coverage = (float)nOccupiedCells / (nGridCols * nGridRows);

    // 5. 综合判断（AND）
    bool condA = totalFeatures  > minFeatures;
    bool condB = nValidDepthPoints > minEffective3D;
    bool condC = coverage        > minGridCoverage;

    if(!condA || !condB || !condC) {
        Verbose::PrintMess("Stereo Init Skipped: N=" + to_string(totalFeatures) +
                        ", Valid3D=" + to_string(nValidDepthPoints) +
                        ", Coverage=" + to_string(coverage), Verbose::VERBOSITY_QUIET);
    }
    return condA && condB && condC;
}
```

### 1.5 移植到其它工程的步骤

1. 在 `Tracking.h` 声明 `bool CheckInitializationQuality(int&, int&);`。
2. 在特征提取器暴露"最大特征数"接口（如 `GetMaxFeatures()`），用于自适应算 `minFeatures`；没有就写死常量。
3. 把上面的函数体拷入 `Tracking.cc`。
4. 在双目/RGBD 初始化入口，把 `if(mCurrentFrame.N > 500)` 替换成 `if(CheckInitializationQuality(nValid3D, nCells))`。
5. 按自己相机/场景调参：
   - **弱纹理/远距离场景**：调低 `minEffective3D`、`minGridCoverage`；
   - **要求高精度**：调高三项阈值；
   - 网格分辨率 `nGridCols/nGridRows` 可改（8×8 更宽松、16×16 更严格）。

> 通用价值：这套"数量 + 有效深度 + 空间分布"的三维质量门控，与相机型号无关，任何双目/RGBD SLAM 的初始化都能直接借鉴，能显著降低"劣质初始地图"导致的早期跟丢。

---

## 二、特征匹配鲁棒性改进（抗抖动 / 抗模糊）

### 2.1 设计思想

抖动会让特征点在帧间位移变大、金字塔层级被误判；模糊会让描述子相似度普遍下降。原始的严格 Ratio Test 和小搜索窗会把大量正确匹配误杀。改进思路：**主动放宽匹配约束，用数量换鲁棒性**。

### 2.2 具体位置与参数

文件：[ORBmatcher.cc](file:///home/dl/MF-SLAM/src/ORBmatcher.cc#L49-L56) `SearchByProjection`

| 改动 | 原始 | 改进 | 位置 |
|------|------|------|------|
| Ratio Test | `mfNNratio`≈0.6~0.7 | `robust_mfNNratio = 0.92f` | [L51](file:///home/dl/MF-SLAM/src/ORBmatcher.cc#L51) |
| 搜索半径倍率 | 无 | `vibration_padding = 2.5f`（左目），长焦右目 ×1.2 | [L55](file:///home/dl/MF-SLAM/src/ORBmatcher.cc#L55)、[L78](file:///home/dl/MF-SLAM/src/ORBmatcher.cc#L78)、[L152](file:///home/dl/MF-SLAM/src/ORBmatcher.cc#L152) |
| 金字塔搜索层 | `[Level-1, Level]` | `[Level-1, Level+1]` | [L80-L87](file:///home/dl/MF-SLAM/src/ORBmatcher.cc#L80-L87) |

### 2.3 移植提示

这是纯参数级改动，非常容易移植：找到你工程里投影匹配的 Ratio 阈值、搜索半径、金字塔层范围三处，按上表放宽即可。注意**放宽会引入更多误匹配**，需配合后端的鲁棒核（Huber）和外点剔除，否则精度会下降。适用于任何"高动态 / 运动模糊"平台（无人机、手持、车载颠簸）。

---

## 三、后端位姿优化改进

文件：[Optimizer.cc](file:///home/dl/MF-SLAM/src/Optimizer.cc#L980-L1096) `PoseOptimization`

- 优化迭代次数从 `{10,10,10,10}` 改为 `{10,7,5,5}`（[L1085](file:///home/dl/MF-SLAM/src/Optimizer.cc#L1085)）——降低计算量、保证实时性。
- 代码中保留了大量**实验性**注释方案（未启用），思路值得参考：
  - 长焦不确定性惩罚因子 `tele_tracking_penalty = 0.8`（对长焦观测降权）；
  - 图像边缘特征降权（边缘畸变大，`weight *= 0.5`）；
  - Student-T 鲁棒核（比 Huber 更重尾，抗强外点）。

> 移植思路：多目/长短焦系统里，对"不可靠相机（长焦/边缘/大畸变）"的观测在信息矩阵上主动降权，是提升鲁棒性的通用手段。

---

## 四、双目匹配与三角化改进

文件：[Frame.cc](file:///home/dl/MF-SLAM/src/Frame.cc)

- `ComputeDifferentfocalStereoMatches()`（[L2077](file:///home/dl/MF-SLAM/src/Frame.cc#L2077)）：重写为基于新双目模型（`mvLeftToRightMatch`），加入**极线约束**（`dy = |kpL.y - kpR.y| > 3` 直接剔除），描述子距离阈值 `TH_LOW = 60`，匹配成功后 `UnprojectStereo` 生成 `mvStereo3Dpoints`。
- 新增三角化函数 `Frame::Triangulate()`（[L2159](file:///home/dl/MF-SLAM/src/Frame.cc#L2159)）：标准 DLT + SVD 求解 3D 点。
- 新增成员：[Frame.h](file:///home/dl/MF-SLAM/include/Frame.h#L302-L315) `mvStereoDepth`、`Triangulate()` 声明。
- 健壮性：`Frame` 构造入口增加空图/空特征/空金字塔检查，防止扑翼场景偶发的坏帧崩溃。

---

## 五、长短焦目标区域定位（NCC）改进

文件：[Ncc.cc](file:///home/dl/MF-SLAM/src/Ncc.cc#L132) `FindPoints`

- 重写为：灰度兼容 → 尺度对齐（长焦缩到短焦尺度）→ **极线 band 约束的 ROI 搜索**（只在中心行附近上下 `searchBand=10` 内做 `matchTemplate`），修复了原版的崩溃问题，并大幅缩小搜索范围提速。
- 文件内保留多个实验版本（注释）：多尺度金字塔 coarse-to-fine、LK 光流精匹配、HHO 鹰群优化亚像素精修——可作为进一步精度优化的思路储备。

---

## 六、性能优化（通用，与平台无关）

文件：[MapPoint.cc](file:///home/dl/MF-SLAM/src/MapPoint.cc)

- `ComputeDistinctiveDescriptors`：
  - 栈上变长数组 `float Distances[N][N]` → 堆 `std::vector<std::vector<int>>`（**防止 N 大时栈溢出**）；
  - 求中位数 `sort` → `std::nth_element`（O(N log N) → O(N)）。
- `UpdateNormalAndDepth`：`cv::Mat` 逐点运算 → `cv::Point3f`（减少 Mat 分配开销），并对金字塔层级索引加边界检查。

> 这两处是纯工程优化，任何 ORB-SLAM 衍生工程都建议直接采用。

---

## 七、新增分析/度量工具（用于评估退化）

这些是"观测工具"，不影响主流程，但对研究扑翼场景退化很有用：

- **可观测性度量**：[Tracking.cc](file:///home/dl/MF-SLAM/src/Tracking.cc#L1267-L1269) 每帧构建 6×6 信息矩阵 H = ΣJᵀΣ⁻¹J，做特征值分解，输出到 `observability_log.txt`（最小特征值、条件数、log-det、trace、观测数）。
- **退化/跟丢统计**：[Tracking.cc](file:///home/dl/MF-SLAM/src/Tracking.cc#L2562) 跟踪失败时记录失败率到 `degeneracy.txt`。
- **耗时统计**：[Tracking.cc](file:///home/dl/MF-SLAM/src/Tracking.cc#L933) 累计跟踪总耗时 `t3`。
- 配套 Python 分析脚本（改进版根目录）：`analyze_observability.py`、`compare_observability.py`、`gemini_*.py` 等。

> 注意：日志路径当前**硬编码**为 `/home/ubuntu/MF-SLAM/...`，移植时务必改成自己的路径或做成配置项。

---

## 八、待接入模块：垂直稳像器（VerticalStabilizer）

- 文件：[VerticalStabilizer.h](file:///home/dl/MF-SLAM/include/VerticalStabilizer.h) / [VerticalStabilizer.cc](file:///home/dl/MF-SLAM/src/VerticalStabilizer.cc)，已在 [CMakeLists.txt](file:///home/dl/MF-SLAM/CMakeLists.txt#L110-L111) 参与编译。
- 功能：针对扑翼高频上下抖动做**垂直方向稳像**。
  1. `goodFeaturesToTrack` 检测角点；
  2. LK 光流跟踪，取 Y 方向位移**中值**（抗误匹配）；
  3. 一阶低通滤波（截止频率 1.5Hz）分离高频抖动分量：`mAlpha = exp(-2π·fc/fs)`；
  4. `warpAffine` 对左右目做整数像素垂直补偿，并标记稳像后无效边带。
- **现状**：类已实现，但主流程（Tracking 等）尚未 `#include` 调用，属于"已实现未接入"。若要启用：在 `GrabImageStereo` 拿到左右灰度图后、送入 `Frame` 之前，调用 `OnlineVerticalStabilizer::Process()`，用返回的稳像图像替换原图，并用 `invalidTop/invalidBottom` 屏蔽无效边带上的特征。

> 通用价值：任何存在周期性/高频机身振动的平台（扑翼、旋翼、手持），都可以用"光流中值估计位移 + 低通滤波分离高频 + 仿射补偿"这套轻量方案做前端稳像，无需 IMU。

---

## 九、工程/兼容性修改（非算法本质，移植时按需）

- **OpenCV 4 适配**：`CV_GRAY2BGR`→`cv::COLOR_GRAY2BGR`、`CV_LOAD_IMAGE_UNCHANGED`→`cv::IMREAD_UNCHANGED`、`CV_FONT_*`→`cv::FONT_*`、`CV_REDUCE_SUM`→`cv::REDUCE_SUM`、`std::chrono::monotonic_clock`→`steady_clock`。涉及 Tracking.cc / LoopClosing.cc / Map.cc / FrameDrawer.cc / Drawkp.cc / Sim3Solver.cc。
- **PnPsolver 全面重写**：旧 C 风格 `CvMat`/`cvSVD`/`cvSolve`/`cvInvert` → `cv::Mat` + `cv::SVD::compute`/`cv::solve`/`cv::invert`。见 [PnPsolver.cc](file:///home/dl/MF-SLAM/src/PnPsolver.cc)、[PnPsolver.h](file:///home/dl/MF-SLAM/include/PnPsolver.h)。
- **构建配置**：[CMakeLists.txt](file:///home/dl/MF-SLAM/CMakeLists.txt) C++11 → C++14；OpenCV 3 → 4；Eigen 版本要求放宽。
- **其它**：[LoopClosing.h](file:///home/dl/MF-SLAM/include/LoopClosing.h#L50-L53) `KeyFrameAndPose` 去掉 aligned_allocator；[System.h](file:///home/dl/MF-SLAM/include/System.h#L23) 打开 `#define SAVE_TIMES`。

---

## 十、完全未改动的文件（无需关注）

Atlas、Converter、G2oTypes、ImuTypes、Initializer、KeyFrame、KeyFrameDatabase、LocalMapping、MapDrawer、MLPnPsolver、OptimizableTypes、ORBextractor.cc、Points、TwoViewReconstruction、Viewer，以及 CameraModels/、Thirdparty/。

---

## 移植优先级建议

| 优先级 | 改进 | 通用性 | 理由 |
|--------|------|--------|------|
| ★★★ | 初始化质量门控（第一节） | 极高 | 与相机无关，直接提升初始地图质量 |
| ★★★ | MapPoint 性能优化（第六节） | 极高 | 纯工程优化，有益无害 |
| ★★☆ | 特征匹配放宽（第二节） | 高（高动态平台） | 参数级，易移植，需配合外点剔除 |
| ★★☆ | 垂直稳像思路（第八节） | 高（振动平台） | 轻量、无需 IMU |
| ★☆☆ | 后端观测降权（第三节） | 中（多目/长短焦） | 多为实验代码，需自行验证 |
| ★☆☆ | 可观测性度量（第七节） | 中（做研究/评估） | 分析工具，非功能改进 |
