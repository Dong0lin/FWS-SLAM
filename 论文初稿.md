# 面向扑翼飞行机器人的语义增强单目视觉SLAM

## 摘要

针对扑翼飞行机器人在户外航拍场景中面临的高频振动干扰、动态目标污染和单目尺度退化三大挑战，本文提出一种语义增强的单目视觉SLAM系统。该系统以ORB-SLAM3为基础框架，融合深度学习目标检测与多层次的语义信息利用，构建了完整的动态环境感知与自适应定位方案。首先，集成TensorRT加速的YOLOv11检测器实现10类航拍目标的实时识别，并提出特征点驱动的二维到三维检测框生成方法与足迹重合去重、数量守恒、观测冻结三重去重机制，构建持久化语义地标。其次，设计基于帧间运动统计的振动量化模型（旋转角+垂向位移双指标）与两阶段时序对齐的自适应跟踪策略，通过“预测性放宽—确认性放宽”机制有效提升强振动条件下的跟踪鲁棒性。再次，构建“检测框预过滤—极线约束分类—贝叶斯概率演化”三层动态点检测管线，结合MapPoint四态语义模型和信息矩阵分层权重调制，实现动态特征的精准剔除与语义权重的分层调制。最后，提出双遍SVD地面平面拟合、尺度健康指标感知、平面引导三角化和语义车辆高度统计冻结机制，为单目系统提供尺度锚定，抑制转弯等低视差场景下的尺度退化。实验结果表明，本系统在动态航拍场景下的定位精度和跟踪鲁棒性较ORB-SLAM3有显著提升，同时保持了嵌入式平台上的实时运行能力。

**关键词**：视觉SLAM；扑翼飞行机器人；语义感知；动态环境；平面约束；振动自适应

---

## 1 引言

扑翼飞行机器人（Flapping-Wing Aerial Vehicle, FWAV）是受自然界鸟类和昆虫飞行启发而发展的一类新型飞行器[1]。与传统的固定翼和多旋翼飞行器相比，扑翼机器人通过翅膀的周期性扑动同时产生升力和推力，具备悬停、低速机动和强抗风能力，在复杂环境侦察、城市峡谷导航、灾后搜救和近距离目标观测等任务中展现出独特优势。近年来，随着微型电机、柔性材料和仿生控制技术的进步，扑翼飞行机器人的实用化进程显著加速，其搭载视觉传感器执行自主导航任务的需求日益迫切。

同步定位与地图构建（Simultaneous Localization and Mapping, SLAM）是移动机器人实现未知环境自主导航的核心技术。视觉SLAM因传感器成本低、信息丰富、无需外部基础设施等优点，成为飞行机器人导航领域的研究热点。以ORB-SLAM3[2]为代表的特征点法视觉SLAM系统经过近十年发展，已具备较高的定位精度和完整的工程实现，支持单目、双目、RGB-D及视觉-惯性多传感器融合，并通过Atlas地图集架构实现了多地图管理与回环重定位。然而，ORB-SLAM3的设计面向一般性场景，其核心假设——场景为静态刚性世界、传感器平台运动平稳——在扑翼飞行机器人的户外航拍场景中遭到严重违反。

具体而言，将ORB-SLAM3直接应用于扑翼飞行机器人（飞行高度约50 m，速度约10 m/s，摄像头斜向下）时，面临三个相互耦合的核心挑战：

**（1）高频振动导致跟踪不稳定。** 扑翼机器人的翅膀扑动频率通常为5~20 Hz，每个扑动周期内机体经历剧烈的姿态变化，传导至相机产生周期性的旋转抖动（典型幅度0.5°~3°）和垂向位移抖动。这种高频振动带来多方面影响：帧间特征匹配质量显著下降（模糊和几何畸变导致描述子失配）；恒速运动模型假设在振动帧间完全失效；位姿估计的旋转分量被振动污染，进而影响三角化精度；更严重的是，振动帧可能被错误判定为需要插入关键帧（因为跟踪质量指标下降触发了关键帧插入条件），导致低质量观测污染全局地图，形成"振动→错误关键帧→地图退化→跟踪更差"的恶性循环。

**（2）动态目标污染地图与位姿估计。** 户外航拍场景中不可避免地存在大量运动目标：行驶的车辆、行走的行人、骑行的自行车等。从50 m高空俯瞰，这些目标在图像中占据显著面积，其上可提取大量ORB特征点。这些动态特征点违反SLAM系统的静态世界假设——它们在世界坐标系中的位置随时间变化，若不加区分地参与位姿优化（PnP求解）和地图构建（三角化、BA），将引入系统性误差，轻则降低定位精度，重则导致跟踪完全丢失。传统的RANSAC剔除外点机制在动态目标面积占比较大时（如路口、停车场场景）效果有限，因为动态内点可能超过RANSAC的容忍比例。

**（3）单目尺度退化。** 单目SLAM固有的尺度不确定性在飞行机器人航拍场景中表现为特殊的退化模式。当飞行器进行转弯（近纯旋转运动）或短暂悬停时，帧间视差极小（视差角<0.1°），此时三角化深度对噪声极度敏感，产生系统性的深度偏小现象。由于SLAM地图的尺度由三角化深度锚定，深度偏小直接导致地图尺度收缩，表现为轨迹"下坠"和地图点"塌缩"。这种退化在长时间飞行中不断累积，最终使系统完全失效。ORB-SLAM3缺乏对这种尺度退化的感知和补偿机制。

上述三个挑战并非独立存在，而是相互耦合、彼此加剧：振动降低匹配质量→匹配质量差导致动态点分类不准→不准确的分类使动态点参与三角化→退化的三角化加剧尺度漂移→尺度漂移进一步恶化跟踪。因此，需要一套系统性的解决方案，而非针对单一问题的独立补丁。

针对上述问题，本文提出一种面向扑翼飞行机器人的语义增强单目视觉SLAM系统（FWS-SLAM, Flapping-Wing Semantic SLAM）。该系统以ORB-SLAM3为基础框架，通过引入深度学习目标检测构建语义感知能力，并围绕语义信息设计了多层次的自适应策略。主要贡献如下：

（1）提出振动量化与两阶段自适应跟踪策略。定义帧间旋转角和垂向位移双指标量化振动强度，通过前200帧中位数自学习建立基准阈值，进而设计"预测性放宽—确认性放宽"的时序对齐机制。该机制利用扑翼振动的时间连续性，以上一帧振动预判当前帧状态实现匹配前参数调节，以当前帧实际振动确认跟踪判定条件，实现搜索半径、匹配阈值和关键帧策略的渐进式自适应调节，兼顾鲁棒性与安全性。

（2）构建三层动态点检测与语义权重体系。设计"检测框预过滤（O(1)复杂度快速筛选）→极线约束分类（帧内统计自适应阈值+振动补偿）→贝叶斯概率演化（Beta共轭+EMA平滑）"的由粗到精三层管线，实现动态特征的精准识别与剔除。提出MapPoint四态语义模型（STATIC/DYNAMIC/SEMANTIC/UNKNOWN），并通过信息矩阵权重分层调制后端BA优化，使语义信息从前端检测贯穿至后端优化。

（3）提出语义增强的地面平面约束与尺度锚定方法。设计双遍SVD地面平面拟合算法（全局初始+低点精化），建立尺度健康指标λ实时感知退化状态，实现平面引导三角化（低视差深度修正）和BA平面软约束（点级+帧级），并创新性地提出语义车辆高度统计冻结机制——在尺度健康期统计车辆点离面高度，在退化期以冻结高度为BA提供强约束锚点。

（4）设计特征点驱动的持久化三维语义地标生成方法。摒弃传统射线-平面求交定位（对振动敏感），改以BA优化后的地图点中位数投影作为三维框定位来源，结合足迹重合比例去重（等面积圆透镜公式）、视野内数量守恒和观测冻结三重机制，构建唯一、稳定、可长期维护的语义级三维场景表达。

## 2 相关工作

### 2.1 视觉SLAM基础

视觉SLAM的核心任务是从图像序列中同时估计相机运动轨迹和重建环境三维结构。根据前端特征提取方式的不同，主流方法可分为特征点法和直接法两大类。特征点法通过提取稀疏特征点并建立帧间匹配关系进行位姿估计，代表工作包括ORB-SLAM系列[2][10]、LSD-SLAM和DSO等；直接法则利用像素灰度信息最小化光度误差，代表工作包括LSD-SLAM和DSO。在户外场景下，特征点法因对光照变化鲁棒、计算复杂度可控等优势，仍是工程实践的主流选择。

ORB-SLAM3[2]是当前最完整的开源特征点法视觉SLAM系统，支持单目、双目、RGB-D及视觉-惯性多传感器融合，采用Atlas地图集实现多地图管理与回环重定位。其前端采用ORB特征[10]提取与描述，通过恒速运动模型预测和参考关键帧匹配实现帧间跟踪，后端使用g2o[13]图优化框架进行局部BA和全局BA，回环检测基于DBoW2词袋模型。然而，该系统的设计基于两个核心假设：场景为静态刚性世界，传感器平台运动平稳。这两个假设在扑翼飞行机器人的户外航拍场景中均遭到严重违反，导致系统性能急剧下降。

### 2.2 语义SLAM与动态环境处理

动态环境下的SLAM是近年来的研究热点[14]。现有方法根据语义信息的利用深度可分为三类：

**基于实例分割的方法。** DynaSLAM[3]利用Mask R-CNN[12]对每帧图像进行实例分割，结合多视图几何验证剔除动态点，并通过背景修复生成静态地图。该方法在动态室内场景中表现优异，但Mask R-CNN的推理耗时较大（GPU上约100 ms/帧），难以满足嵌入式平台的实时性要求。此外，实例分割对高空航拍中小目标的分割质量有限，边界精度不足以支撑精细的动态区域判定。

**基于目标检测的方法。** YOLO-SLAM[4]等工作将轻量级目标检测器引入SLAM前端，利用检测框粗略划定动态区域。这类方法实时性较好，但存在以下不足：（1）仅将检测框作为二值掩模使用，未充分利用类别、置信度等语义信息；（2）缺乏对检测结果的持久化利用（如三维地标构建）；（3）未考虑平台自身振动对动态判定的干扰。GC-SLAM[5]通过网格化置信度评估动态区域，结合几何一致性验证，但未考虑平台振动的影响，且其网格化策略在高空航拍中因目标尺度小而效果受限。

**基于语义分割的方法。** 部分工作[15]利用语义分割网络为每个像素赋予语义标签，进而区分可移动/不可移动区域。这类方法精度最高，但计算开销最大，且对训练数据的场景覆盖度要求较高，在航拍视角下的泛化能力有待验证。

综合来看，现有动态SLAM方法多针对地面机器人或室内场景设计，对高空航拍中目标尺度小、检测精度有限、平台振动剧烈的特殊情况缺乏针对性处理。本文选择目标检测（而非实例分割或语义分割）作为语义感知手段，在精度与效率之间取得平衡，并通过多层次的后处理机制弥补检测精度的不足。

### 2.3 飞行机器人SLAM

飞行机器人SLAM面临与地面机器人不同的特殊挑战：运动速度快、视场角大、场景深度范围大、缺乏近距离纹理等。FW-ORB-SLAM[6]专门针对扑翼飞行机器人提出图像增强和初始化优化策略，通过自适应对比度增强提升低纹理场景的特征提取质量，并设计多条件初始化验证提高初始化成功率。然而，该工作未涉及动态目标处理和语义信息利用，且其振动处理仅限于图像增强层面，未从跟踪策略层面进行自适应设计。其他UAV-SLAM工作[7]多关注路径规划与建图融合，对单目尺度退化问题缺乏有效手段，对平台振动的处理也多限于IMU补偿，未从视觉算法层面设计鲁棒策略。

### 2.4 平面约束与尺度锚定

地面平面作为航拍场景中最显著的几何先验，已被用于辅助SLAM尺度恢复[8]。现有平面辅助SLAM工作可分为三类：（1）基于IMU的平面估计，通过重力方向确定平面法向，但依赖额外传感器；（2）基于点云拟合的平面估计，通过RANSAC或SVD从三维点云中提取平面，但受动态点和尺度退化的影响；（3）基于深度学习的平面检测，从图像中直接回归平面参数，但泛化能力受限。然而，现有工作多假设平面参数已知或一次性估计后不再更新，缺乏从纯视觉观测中自动估计、动态维护并利用语义信息增强约束强度的完整方案。特别是，在单目尺度退化发生时，平面约束本身也会受到影响，如何感知退化状态并自适应调节约束强度，是尚未解决的问题。

### 2.5 本文定位

综上所述，现有方法难以同时应对扑翼飞行机器人场景中的振动干扰、动态目标和尺度退化三重挑战。本文通过多层次的语义信息利用与自适应策略设计，构建了一套面向该特殊平台的完整解决方案。与现有工作相比，本文的核心区别在于：（1）不仅将语义信息用于动态剔除，还将其用于尺度约束和三维地标构建，实现语义信息的全链路利用；（2）针对扑翼振动设计了从指标量化到策略调节的完整自适应体系；（3）提出尺度健康感知与语义锚定的联合机制，实现对尺度退化的主动防御而非被动修复。

## 3 系统总体架构

本系统在ORB-SLAM3的三线程架构（Tracking、Local Mapping、Loop Closing）基础上，新增独立的Detection检测线程，形成四线程并行架构。系统总体数据流如图1所示。

**检测线程（Detection Thread）。** 该线程独立运行YOLOv11 TensorRT推理引擎，与Tracking线程通过condition_variable事件驱动机制同步。具体工作流程为：Tracking线程获取新图像后，通过共享缓冲区提交图像并调用`notify_one()`唤醒检测线程；检测线程执行GPU端预处理（Letterbox缩放、BGR→RGB、归一化、HWC→CHW）和TensorRT FP16推理，将检测结果（包含边界框坐标、类别ID、置信度和运动概率）写入共享缓冲区后通知Tracking线程。这种事件驱动设计相比传统的usleep轮询方式，消除了微秒级延迟抖动和CPU空转，在Jetson嵌入式平台上尤为重要（CPU资源紧张）。检测线程与Tracking线程完全解耦，检测失败或超时不会阻塞跟踪主流程。

**Tracking线程。** 在标准ORB-SLAM3跟踪流程（特征提取→帧间匹配→位姿优化→关键帧决策）中嵌入以下新增模块：
- 图像增强预处理：局部自适应对比度增强 + 导向滤波去噪
- 振动指标计算与自适应参数调节：帧间旋转角/垂向位移提取、中位数阈值自学习、两阶段参数调节
- 动态一致性检测与语义点分类：检测框预过滤、极线约束分类、贝叶斯概率更新、MapPoint状态转换
- 平面数据收集与维护：车辆语义点收集、双遍SVD拟合、尺度健康指标计算、动态偏移更新
- 2D→3D检测框生成：特征点驱动定位、三重去重、RANSAC对齐、车辆高度统计

上述模块并非每帧全部执行。动态一致性检测每帧执行（但贝叶斯更新每20帧一次）；平面维护每30帧执行一次；3D框生成每30帧执行一次。这种分频执行策略在保证效果的同时控制了计算开销。

**Local Mapping线程。** 在标准的关键帧插入、局部BA和新点三角化流程基础上，新增平面引导三角化模块。当三角化视差角极小时，利用平面先验修正深度估计，并对修正后的点预标记较高的平面约束权重，使其在后续BA中获得更强的平面约束。

**Loop Closing线程。** 保持ORB-SLAM3原有功能不变，包括回环检测、Sim3对齐和全局BA。平面约束在全局BA中同样生效，保证回环修正后的地图仍满足平面几何一致性。

**Atlas地图集。** 在原有地图点和关键帧基础上，新增两类持久化地图元素：
- 地面平面模型：包含法向量 $\mathbf{n}$、参考偏移 $d_{ref}$、动态偏移 $d_{dyn}$、尺度健康指标 $\lambda$、拟合时刻相机高度 $H_{cam}$，以及平面有效标志
- 持久化三维检测框集合：每个框包含三维位置、尺寸、类别、观测计数、冻结状态、融合历史等信息

线程间同步采用condition_variable + mutex的事件驱动模式，替代ORB-SLAM3原始的usleep轮询。这不仅降低了CPU占用，还确保了检测结果与图像帧的严格时序对齐，避免了轮询方式下可能出现的帧-检测错配问题。

## 4 语义目标检测与三维地标构建

### 4.1 TensorRT实时检测管线

本系统集成YOLOv11目标检测器，使用VisDrone2019数据集[9]训练，覆盖10类航拍目标：行人(pedestrian, class 0)、人群(people, class 1)、自行车(bicycle, class 2)、轿车(car, class 3)、货车(van, class 4)、卡车(truck, class 5)、三轮车(tricycle, class 6)、篷车(awning-tricycle, class 7)、公交车(bus, class 8)和摩托车(motor, class 9)。该数据集由无人机在多种场景（城市、乡村、高速公路）下采集，包含不同高度、角度和光照条件下的目标标注，与本系统的应用场景高度匹配。

推理采用TensorRT FP16精度，充分利用Jetson平台的GPU算力。整个检测管线在GPU端完成从原始图像到检测结果的端到端处理，包括：
- Letterbox缩放：保持长宽比缩放至网络输入尺寸（640×640），边缘填充灰色像素
- 色彩空间转换：BGR→RGB
- 归一化：像素值从[0,255]映射到[0,1]
- 通道重排：HWC→CHW格式
- NMS后处理：置信度阈值0.25，IoU阈值0.45

检测线程与Tracking线程通过条件变量实现事件驱动同步：Tracking线程提交图像后调用`notify_one()`唤醒检测线程，检测完成后通过共享缓冲区传递结果。这种设计避免了微秒级轮询带来的延迟抖动和CPU资源浪费，在Jetson等CPU资源紧张的嵌入式平台上尤为重要。检测结果包含每个目标的边界框坐标 $(u_1, v_1, u_2, v_2)$、类别ID $c$、置信度 $s$ 和运动概率 $p_{mov}$，其中运动概率由类别先验确定（行人/人群为0.8，车辆类为0.5，其他为0.3）。

### 4.2 特征点驱动的2D→3D框生成

传统的2D→3D框提升方法通常采用检测框底边中心像素的射线与地面平面求交进行定位，即 $P = O_c + t \cdot \mathbf{d}$，其中 $\mathbf{d}$ 为像素射线方向，$t$ 由平面方程 $\mathbf{n} \cdot P = d_{ref}$ 解出。然而，这种方法存在固有缺陷：在扑翼振动条件下，像素坐标的微小偏移（即使仅1~2像素）经射线放大后（飞行高度50 m）导致三维位置产生显著误差（可达数米）。此外，单像素定位无法利用检测框内多个特征点的冗余信息，鲁棒性不足。

本文提出以BA优化后的地图点作为定位来源的特征点驱动方法。其核心思想是：地图点已经过多帧观测和BA优化，其三维坐标精度远高于单帧像素坐标的射线投影。具体步骤如下：

**物体点筛选。** 对于每个检测框，收集框内满足"平面之上"约束的地图点：

$$0.001 < \mathbf{n} \cdot P - d_{ref} < \gamma \cdot H_{cam} \tag{1}$$

其中 $\mathbf{n}$ 为地面法向量，$d_{ref}$ 为平面参考偏移，$H_{cam}$ 为SLAM尺度下的相机离地高度，$\gamma=0.5$ 为最大高度比例系数。下界0.001排除地面上的点（属于背景而非目标），上界排除异常高点（如误匹配到天空或远处建筑的点）。仅当物体点数量 $N \geq 4$ 时生成三维框，否则认为观测不足。人类目标（class 0/1）和动态标记目标直接跳过，因为行人位置不固定，不适合作为持久化地标。

**位置估计。** 将物体点投影到平面后，逐坐标取中位数作为框底面中心：

$$P_{ctr} = \mathrm{med}\{P_i - \mathbf{n}(\mathbf{n} \cdot P_i - d_{ref})\}_{i=1}^{N} \tag{2}$$

中位数估计对离群点（如背景误匹配点、动态目标上的残留点）具有天然的鲁棒性，其击穿点（breakdown point）为50%，即即使将近一半的点为离群点，中位数仍能给出合理估计。相比均值估计，中位数无需额外的RANSAC外点剔除步骤，计算效率更高。

**尺寸估计。** 在平面局部坐标系 $(U, V)$ 下计算物体点分布的5%~95%百分位范围，并与类别物理尺寸先验加权融合：

$$\alpha = \min(1, N/20) \tag{3}$$

$$W = \alpha \cdot (u_{95\%} - u_{5\%}) + (1-\alpha) \cdot W_{prior} \tag{4}$$

其中 $\alpha$ 为融合权重，点越多越信任特征点分布。当 $N=20$ 时 $\alpha=1$，完全使用特征点分布；当 $N=4$（最低门限）时 $\alpha=0.2$，主要依赖先验。这种设计使得在观测稀疏时（远距离、小目标）先验起主导作用，观测充分时自动过渡到数据驱动。深度 $D$ 和高度 $H$ 同理计算，最终尺寸截断在 $[0.2, 3.0] \times$ 先验值范围内，防止极端估计。

**类别先验尺寸表。** 各类别的物理尺寸先验 $(W_{prior}, D_{prior}, H_{prior})$ 根据VisDrone数据集中目标的平均统计尺寸设定，例如轿车约(1.8, 4.5, 1.5) m，公交车约(2.5, 12.0, 3.2) m，行人约(0.5, 0.5, 1.7) m。这些先验在SLAM尺度下需乘以尺度因子 $s = H_{cam}/H_{real}$ 进行转换。

### 4.3 三重去重机制

持久化三维地标的核心难题是防止同一目标被重复生成。在连续多帧观测中，同一辆车会在不同帧产生多个检测框，若不加去重则地图中将充斥大量重复地标。本文设计三重递进式去重机制：

**（1）足迹重合去重。** 将三维框足迹（即框在地面上的投影）近似为等面积圆（半径 $r = \sqrt{WD/\pi}$），通过圆-圆相交透镜公式计算重合比例：

$$R_{overlap} = \frac{A_{intersect}(r_1, r_2, \|c_1 - c_2\|)}{\pi \min(r_1, r_2)^2} \tag{5}$$

其中透镜面积 $A_{intersect}$ 由两圆半径和圆心距确定。分母取较小圆的面积，使得小框被大框包含时重合比例接近1。判定规则如下：
- 同类别且 $R_{overlap} > 0.3$：判定为同一目标，执行移动平均融合（位置、尺寸按观测次数加权）
- 存在任意重合但不满足同类/阈值条件：丢弃新框（跨类别抑制，防止检测器类别跳变产生重复）
- 零重合：视为新目标，允许创建

阈值0.3的选取考虑：过小（如0.1）会将相邻但不同的目标误合并（如停车场中相邻车辆）；过大（如0.5）则对位置估计误差容忍不足，导致同一目标因定位偏差而未被合并。

**（2）数量守恒。** 当前视野（FOV）内某类三维框数量不得超过该类二维检测框数量：

$$N^c_{3D}(\mathrm{FOV}) \leq N^c_{2D} \tag{6}$$

该约束的物理含义是：三维框是二维检测的持久化表达，其数量不应超过当前观测到的目标数。超出配额时禁止新建框，并在事后清理中删除观测次数最少的未冻结框。这一机制有效防止了检测器偶发的假阳性累积。

**（3）观测冻结。** 累计观测达到3次后框被冻结，位置和尺寸永久封存，不再参与融合更新和RANSAC对齐，仅累计观测计数。冻结机制的设计动机：（1）防止多次融合后位置漂移——每次融合都引入少量误差，累积后可能使框偏离真实位置；（2）冻结框在数量清理中受到保护，防止检测器偶发漏检导致真实目标被误删；（3）冻结框不参与RANSAC对齐，避免已确认位置的目标被全局拟合拉偏。

### 4.4 RANSAC直线对齐

利用停车场、马路边缘等场景中车辆呈直线排列的几何先验，对同类未冻结框执行RANSAC直线拟合。具体步骤：随机选取2个框中心确定一条直线，计算其余框中心到该直线的距离，距离小于框宽度一半的计为内点。迭代50次后，当内点率超过0.6且内点数不少于3时，将内点框向拟合直线方向收拢30%：

$$P_{new} = P_{old} + 0.3 \cdot (P_{proj} - P_{old}) \tag{6'}$$

其中 $P_{proj}$ 为框中心在拟合直线上的投影点。收拢比例30%为保守取值，仅部分修正而非完全强制对齐，避免在几何先验不成立的场景中产生过大偏差。

## 5 振动自适应跟踪

扑翼飞行机器人的振动具有两个显著特点：（1）时间连续性——相邻帧的振动强度高度相关，因为扑动是周期性运动；（2）不可预测性——虽然整体呈周期性，但受气流、机动动作等影响，单帧振动幅度无法精确预测。本文的自适应策略充分利用第一个特点，同时为第二个特点保留安全余量。

### 5.1 振动指标定义

每帧从帧间相对位姿 $T_{rel} = (R_{rel}, t_{rel})$ 中提取两项振动指标：

$$\theta = |\mathrm{angle}(\mathrm{AngleAxis}(R_{rel}))| \tag{7}$$

$$d_v = |\hat{t}_{rel} \cdot \mathbf{e}_z| \tag{8}$$

其中 $\theta$ 为帧间旋转角（弧度），反映角振动强度；$d_v$ 为归一化平移在垂向（$\mathbf{e}_z$ 为相机坐标系z轴单位向量）的分量，反映线振动强度。采用双指标而非单一指标的原因：纯旋转振动（如偏航抖动）仅体现在 $\theta$ 中，纯垂向振动（如升力波动）仅体现在 $d_v$ 中，两者可能独立发生也可能耦合出现。

需要注意的是，$\theta$ 和 $d_v$ 包含了真实运动和振动的叠加。在正常飞行中，帧间旋转角主要由飞行器转弯贡献；在悬停或直行时，$\theta$ 几乎完全由振动贡献。本文不试图分离两者，而是将超出典型水平的部分视为振动，通过中位数阈值实现自动区分。

### 5.2 中位数阈值自学习

系统启动后的前200帧为初始化学习阶段，收集振动指标序列后取中位数作为基准阈值：

$$T_\theta = \mathrm{med}\{\theta_i\}_{i=1}^{200}, \quad T_d = \mathrm{med}\{d_{v,i}\}_{i=1}^{200} \tag{9}$$

选择中位数而非均值或高百分位数的原因在于：
- 对偶发的强振动帧具有鲁棒性（均值会被极端值拉高）
- 能反映扑翼飞行的典型振动水平（而非极端情况）
- 无需假设振动分布形式（如高斯分布）

需注意，由此约50%的帧会超出阈值，因此后续所有自适应调节均为渐进式而非开关式。这是本设计与传统"振动检测→模式切换"方案的核心区别：不存在硬阈值切换，避免了模式边界处的不连续性和抖动。

200帧的学习窗口选择考虑：太短（50帧）则统计不稳定，受初始化瞬态影响大；太长（500帧）则对飞行状态变化（如从悬停转入巡航）的适应过慢。200帧在典型帧率30fps下约6.7秒，足以覆盖多个扑动周期。

### 5.3 归一化振动等级

定义归一化振动等级 $v$ 为两项指标超出阈值百分比的最大值：

$$v = \max\left(\max\left(0, \frac{\theta - T_\theta}{T_\theta}\right), \max\left(0, \frac{d_v - T_d}{T_d}\right)\right) \tag{10}$$

$v=0$ 表示振动正常（未超出阈值），$v=1$ 表示超出阈值100%（即达到阈值的两倍）。取两项指标的最大值而非加权和，是因为任一维度的强振动都足以影响跟踪质量。实际使用时截断为 $\tilde{v} = \min(v, 2)$，防止极端值导致参数调节过度。

### 5.4 两阶段时序对齐设计

由于振动指标依赖帧间位姿估计，只能在特征匹配完成之后计算。这产生了一个时序矛盾："需要振动信息指导匹配，但振动信息来自匹配之后"。本文利用扑翼振动的时间连续性（相邻帧振动强度高度相关），设计两阶段方案解决该矛盾：

**阶段一：预测性放宽（匹配前）。** 利用上一帧振动等级 $\tilde{v}_{prev}$ 预判当前帧振动水平，在匹配前调整参数：

$$r = r_0(1 + 0.5\tilde{v}_{prev}), \quad r_0 = 15 \Rightarrow r \in [15, 30] \tag{11}$$

$$N_{mm} = \max(12, 20 - 4\tilde{v}_{prev}) \tag{12}$$

$$N_{ref} = \max(9, 15 - 3\tilde{v}_{prev}) \tag{13}$$

其中 $r$ 为投影搜索半径（像素），振动越大搜索范围越宽，因为振动导致恒速模型预测偏差增大；$N_{mm}$ 和 $N_{ref}$ 分别为恒速模型和参考关键帧模式的最低匹配数阈值，振动越大阈值越低，因为强振动确实会减少成功匹配数。

**阶段二：确认性放宽（匹配后）。** 获得当前帧实际振动等级 $\tilde{v}_{cur}$ 后，调整跟踪成功判定条件：

$$N_{map} = \max(6, 10 - 2\tilde{v}_{cur}) \tag{14}$$

$$N_{inlier} = \max(15, 30 - 8\tilde{v}_{cur}) \tag{15}$$

其中 $N_{map}$ 为跟踪成功所需的最少地图点匹配数，$N_{inlier}$ 为PnP RANSAC的最少内点数。所有阈值均设有明确下限，确保即使在极端振动下也不会将完全失败的匹配误判为成功。下限的设定原则：低于该值时位姿估计完全不可信，宁可触发重定位而非继续跟踪。

两阶段设计的合理性在于：阶段一用上一帧信息"预判"，即使预判偏差，也只是搜索范围略大或略小，不会导致跟踪失败；阶段二用当前帧实际值"确认"，保证最终判定基于真实信息。这种"宽进严出"的设计兼顾了鲁棒性与安全性。

### 5.5 关键帧振动抑制与丢失恢复

当振动等级超过2.0（即超出阈值200%）时，拒绝插入关键帧，防止强振动帧的模糊观测污染全局地图。其设计动机：强振动帧的特征点定位精度低、描述子质量差，若作为关键帧插入，其观测将在BA中引入系统性误差，且该误差会通过共视图传播到相邻关键帧。

同时，RECENTLY_LOST状态的超时窗口随振动延长：

$$t_{lost} = 3.0 + 1.5\tilde{v}_{prev} \in [3, 6] \text{ 秒} \tag{16}$$

为振动期间的短暂跟踪失败提供更充裕的恢复时间。标准ORB-SLAM3的丢失超时为固定3秒，在强振动场景下可能过短——振动导致的跟踪丢失往往是短暂的（几个扑动周期后自动恢复），过早进入LOST状态会触发不必要的重定位尝试。

### 5.6 扑翼初始化优化

单目SLAM初始化需要足够的帧间视差和特征匹配数，在扑翼飞行中因振动和快速运动而更易失败。本文设计以下优化：

（1）自适应匹配数阈值 $N_{init} = \max(80, \min(200, 0.2 N_{feat}))$，替代ORB-SLAM3的固定阈值100。当提取特征数较多时（纹理丰富场景）提高阈值以保证初始化质量，特征数较少时（低纹理场景）降低阈值以增加初始化成功机会。

（2）三重初始化质量门控：有效三角化点数检查（确保足够的三维点）、10×10网格空间覆盖率验证（≥8%，防止特征点聚集在图像局部区域导致初始化几何退化）、SVD平面法向合理性验证（确保初始地图的几何结构合理）。

（3）采用FW-ORB-SLAM[6]的局部自适应对比度增强算法，通过纹理增益 $\alpha = 1 + k\sigma/\mu$（其中 $\sigma$、$\mu$ 分别为局部标准差和均值）和导向滤波提升低纹理场景的特征提取质量。该增强仅在特征提取前应用，不影响后续匹配和位姿估计的图像一致性。

## 6 动态一致性检测与语义权重

动态目标处理是本文系统的核心模块之一。与现有方法简单地将检测框内特征点全部剔除不同，本文采用由粗到精的三层管线，在精度与效率之间取得平衡，并通过概率演化实现软判定而非硬切换。

### 6.1 三层动态点过滤管线

**层级一：检测框预过滤。** 利用预计算的逐特征点语义分类结果（每帧在特征提取阶段通过检测框包含测试完成，存储为查找表），以 $O(1)$ 复杂度判定每个帧间匹配对是否落入语义检测框内。具体实现：在特征提取完成后，遍历所有检测框，对框内的每个特征点记录其所属类别（若落入多个框则取置信度最高者）。后续匹配时，只需查询该查找表即可判定匹配对的语义属性。

落入任意检测框（含行人、车辆等所有类别）的匹配对进入语义匹配队列，参与后续动态分类；其余直接进入静态匹配队列。该层级快速排除图像中80%以上远离目标的背景特征点，显著减少后续极线约束计算的开销。值得注意的是，此层级仅做“是否落入检测框”的二值判定，不做动态/静态分类——因为检测框内的目标可能是静止的（如停放的车辆），不应被无条件剔除。

**层级二：极线约束分类。** 对语义匹配队列中的特征点对，利用帧间相对位姿计算基础矩阵：

$$F = K^{-T}[t]_{\times} R K^{-1} \tag{17}$$

计算当前帧特征点 $x_2$ 到对应极线 $l_2 = Fx_1$ 的距离：

$$d_{epi} = \frac{|x_2^T F x_1|}{\sqrt{(Fx_1)_1^2 + (Fx_1)_2^2}} \tag{18}$$

其物理含义：静态点应满足极线约束（$d_{epi}$ 接近零，仅受特征点定位精度影响，典型值<1像素），而动态目标上的点因违反刚性运动假设而产生显著极线距离（可达数十像素）。

分类阈值采用帧内统计自适应策略：

$$\tau = \max(1.0, \; 0.8 \cdot \mathrm{med}\{d_{epi}\} + 1.5\sigma_d) \cdot (1 + 0.6v) \tag{19}$$

其中 $v$ 为当前帧振动等级。该阈值设计的考虑：
- 基本项 $0.8 \cdot \mathrm{med} + 1.5\sigma$：基于帧内统计而非固定值，适应不同场景和运动速度
- 下限 $\max(1.0, \cdot)$：保证至少1像素的容忍度，避免将特征点定位噪声误判为动态
- 振动补偿项 $(1+0.6v)$：扑翼抖动产生的伪运动会增大所有点的极线距离，若不补偿则会将静态点误判为动态。系数0.6为经验值，表示振动对极线距离的影响程度

**层级三：贝叶斯概率演化。** 以Beta-二项共轭形式维护每类目标的动态概率。每20帧统计各类检测框内动态/静态特征点计数后执行更新：

$$p_{raw} = \frac{\alpha_0 + w_d \cdot n_{dyn}}{\alpha_0 + \beta_0 + n_{static} + w_d \cdot n_{dyn}} \tag{20}$$

其中 $\alpha_0 = S \cdot p_{prior}$，$\beta_0 = S \cdot (1-p_{prior})$，先验强度 $S=200$，动态观测权重 $w_d=3$。各参数的设计考虑：
- $S=200$：先验强度较大，使得概率不会因单次观测而剧烈变化，保证稳定性
- $p_{prior}$：由类别运动概率确定（行人0.8，车辆0.5，其他0.3），体现不同类别的先验动态倾向
- $w_d=3$：动态观测权重，使少量动态目标不被大量静态点淹没。例如，一辆行驶的车上可能仅有10个动态点，但周围有200个静态点，若不加权则动态概率会被稀释

后验映射到 $[0.30, 0.60]$ 区间后经指数移动平均平滑：

$$p_t = (1-\eta) \cdot p_{t-1} + \eta \cdot [0.30 + 0.30 \cdot p_{raw}] \tag{21}$$

其中 $\eta=0.25$。映射到[0.30, 0.60]而非[0, 1]的设计动机：避免概率完全饱和到0或1，保留一定的不确定性，使得当目标从运动转为静止（或反之）时概率能够逐渐调整。$\eta=0.25$意味着新观测占25%权重，历史占75%，平滑效果显著，避免单帧异常观测导致状态突变。

概率超过0.5的MapPoint被标记为DYNAMIC，从后续所有SLAM运算中排除。

### 6.2 MapPoint四态语义模型

本文扩展ORB-SLAM3的MapPoint结构，定义四态语义模型：

| 状态 | 含义 | 处理方式 |
|------|------|--------|
| STATIC | 确认的静态点 | 正常参与所有SLAM运算 |
| DYNAMIC | 确认的动态点 | 从三角化、BA、平面统计中完全排除 |
| SEMANTIC | 语义框内待分类点 | 参与极线分类，根据结果转为STATIC或DYNAMIC |
| UNKNOWN | 未分类点 | 正常参与SLAM运算 |

状态转换规则：
- UNKNOWN → SEMANTIC：当点首次被检测到落入语义框内
- SEMANTIC → STATIC：极线约束判定为静态（$d_{epi} < \tau$）
- SEMANTIC → DYNAMIC：贝叶斯概率超过0.5
- DYNAMIC → STATIC：概率回落至0.4以下（目标停止运动）

每个MapPoint还维护以下新增属性：逐点运动概率 `mMovingProbability`、语义类别 `mnSemanticClass`、平面约束权重 `mfPlaneInfo`、语义高度偏移 `mfSemanticHeightOffset`。其中 `mbBad` 标记升级为 `std::atomic<bool>` 实现无锁线程安全访问，因为MapPoint在Tracking、LocalMapping和LoopClosing三个线程中均被访问。

### 6.3 语义权重分层调制

语义信息通过信息矩阵权重直接调制后端BA优化。平面约束边的信息矩阵为：

$$\Omega_P = w_0 \cdot w_{plane}, \quad w_0 = 5.0 \tag{22}$$

其中 $w_{plane}$（即 `mfPlaneInfo`）按语义分层赋值：
- 普通平面点：$w_{plane} = 1.0$（标准约束）
- 平面引导三角化点：$w_{plane} = 3.0$（强约束，因为该点的深度已经平面修正，应更严格地约束其在平面上）
- 冻结车辆语义点：$w_{plane} \geq 3.0$（最强约束，作为尺度锚点）
- 动态点：完全排除，不参与任何优化

这种分层权重设计使得不同来源、不同可信度的点对BA的贡献不同，避免了"一刀切"的约束方式。

## 7 地面平面约束与语义尺度锚定

单目SLAM的尺度不确定性是制约其实际应用的核心难题。在扑翼飞行机器人航拍场景中，尺度退化主要表现为转弯或悬停时的地图尺度收缩。本文提出一套完整的平面约束体系，从平面估计、退化感知、三角化修正到BA约束，形成多层次的尺度稳定性保障。特别是，创新性地利用语义信息（车辆类别）增强平面约束的强度和可靠性。

### 7.1 双遍SVD平面拟合

单目初始化完成后，系统从车辆语义框（car/van/truck/bus，即class 3~8）内收集地图点约5秒，然后执行两遍SVD拟合。选择车辆语义点而非所有点的原因：车辆位于地面上，其特征点天然满足平面约束；而背景点可能包含建筑、树木等非平面结构，会干扰拟合。

**第一遍（全局初始）：** 对所有非动态点执行SVD分解。将点集去均值后构建协方差矩阵，对其执行SVD分解，最小奇异值对应方向为初始法向量 $\mathbf{n}_0$。该步骤给出平面的粗略估计。

**第二遍（低点精化）：** 取沿 $\mathbf{n}_0$ 投影最低的60%点（最接近地面的点集）再次SVD，得到精细法向量 $\mathbf{n}$。取最低60%而非全部的动机：车辆点中包含车顶、车身侧面的点，这些点高于地面，若全部参与拟合会使平面法向偏向车身侧面。取最低60%近似为“地面接触点”，更准确地反映地面方向。

**鲁棒性保护：** 若 $\mathbf{n}$ 与 $\mathbf{n}_0$ 偏离超过25°则回退至 $\mathbf{n}_0$，保证鲁棒性。这种偏离可能发生在低点集不足（如仅有少量车辆点）或存在异常点的情况下。

平面模型采用Hesse标准形式：

$$\mathbf{n} \cdot P = d_{ref} \tag{23}$$

其中 $d_{ref} = \mathbf{n} \cdot \bar{P}_{low}$（低点集质心投影）。同时记录拟合时刻相机高度 $H_{cam} = \mathbf{n} \cdot O_c - d_{ref}$ 作为尺度参考。法向量方向约定为指向相机侧（即 $\mathbf{n} \cdot O_c > d_{ref}$），确保高度为正值。

### 7.2 尺度健康指标

定义尺度健康指标 $\lambda$ 为三角化深度与平面预测深度之比值的中位数：

$$\lambda = \mathrm{med}\{t_{triang} / t_{plane}\} \tag{24}$$

其中 $t_{triang}$ 为纯几何三角化得到的深度，$t_{plane}$ 为利用平面方程预测的深度（即射线与平面交点的深度）。该指标的物理含义：
- $\lambda \to 1$：三角化结果与平面预测一致，尺度健康
- $\lambda < 1$：三角化深度系统性偏小，即发生了尺度退化（典型诱因为转弯低视差运动）
- $\lambda > 1$：三角化深度偏大，可能由动态点污染或平面估计偏差引起

该指标每30帧更新一次，驱动后续BA约束强度的自适应调节：当 $\lambda < 0.85$ 时，认为尺度已发生显著退化，增强平面约束权重；当 $\lambda > 0.95$ 时，尺度健康，维持标准约束。

### 7.3 平面引导三角化

在LocalMapping线程的三角化过程中，当视差角极小（$\cos$ 值接近1）时，纯几何三角化深度不可靠。本文以平面预测深度修正三角化结果：

$$x_{3D} = O_w + \mathbf{dir}(t_{tri} + \alpha(t_{plane} - t_{tri})) \tag{25}$$

其中 $O_w$ 为相机光心，$\mathbf{dir}$ 为射线方向，$t_{tri}$ 为三角化深度，$t_{plane}$ 为平面预测深度。融合权重 $\alpha$ 依视差角分档：
- $\cos > 0.9998$（视差角<1.1°）：$\alpha=0.60$，近纯旋转，三角化几乎不可信
- $\cos > 0.9995$（视差角<1.8°）：$\alpha=0.45$
- $\cos > 0.9990$（视差角<2.6°）：$\alpha=0.30$
- $\cos > 0.9980$（视差角<3.6°）：$\alpha=0.15$
- 其余：不干预，使用纯三角化结果

分档而非连续函数的设计考虑：实现简单、计算开销小，且避免了在临界视差角处的数值不稳定。

此外设计尺度修正分支：当深度比 $s = t_{tri}/t_{plane} < 0.8$ 时，表明三角化深度显著偏小（尺度退化征兆），此时 $\alpha_s = 0.85(1-s)$，取 $\alpha_{final} = \max(\alpha, \alpha_s)$。修正后的点预标记 $w_{plane}=3.0$，在BA中获得强平面约束，进一步稳固修正效果。

### 7.4 BA平面软约束

在全部6个BA入口函数（局部BA、全局BA、回环BA等）中，为平面标记点添加自定义g2o一元边：

**地图点级约束：**

$$e_P = \mathbf{n} \cdot P - (d_{ref} + h_{sem}) \tag{26}$$

其中 $h_{sem}$ 为逐点语义高度偏移（普通点为0，车辆语义点为冻结参考高度 $h_{ref}$）。该约束将地图点约束至平面（或平面之上的固定高度），信息矩阵为 $\Omega_P = 5.0 \cdot w_{plane}$。

**关键帧级约束：**

$$e_{KF} = \mathbf{n} \cdot O_c - d_{ref} - H_{cam} \tag{27}$$

信息矩阵权重为0.05，作为弱约束仅防止相机高度漂移，不过度限制位姿自由度。设计为弱约束的原因：相机高度在飞行过程中确实会变化（如爬升、下降），过强的约束会限制这种合法运动。0.05的权重仅起到"正则化"作用，防止尺度退化时高度估计发散。

### 7.5 语义车辆高度约束

直接将车辆特征点约束至地面平面（$h_{sem}=0$）会产生系统性偏差——车顶和车身特征点本就高于地面，强制约束至地面会拉低这些点，反而引入误差。本文设计统计-冻结-约束三阶段方案解决该问题：

**统计阶段：** 初始化后，在尺度健康期（$\lambda > 0.85$）收集车辆语义点（class 2~9，非动态）的离面高度样本：

$$h_i = \mathbf{n} \cdot P_i - d_{ref}, \quad 0 < h_i < 0.1 H_{cam} \tag{28}$$

上界 $0.1 H_{cam}$ 排除异常高点（如误匹配到建筑或天空的点）。选择在尺度健康期统计的原因：此时三角化深度可靠，统计结果反映真实的车辆高度；若在退化期统计，则统计值本身已被尺度误差污染。

**冻结时机：** 当 $\lambda > 0.85$ 且样本数不少于20时，冻结参考高度：

$$h_{ref} = \mathrm{med}\{h_i\} \tag{29}$$

使用中位数而非均值，对异常样本鲁棒。冻结后不再更新，保证在尺度退化期间约束目标不变。20个样本的最低要求确保统计的可靠性。

**约束阶段：** 冻结后，所有车辆语义点在BA中约束至 $\mathbf{n} \cdot P = d_{ref} + h_{ref}$，权重 $w_{plane} \geq 3.0$。其物理含义：车辆特征点应位于地面之上固定高度处，该高度在尺度健康时确定，在退化时作为“锚点”抵抗尺度漂移。

这种"尺度健康时统计、退化期强约束"的策略，使车辆点成为对抗尺度退化的"语义锚点"。与传统的IMU尺度约束相比，本方法无需额外传感器，且约束强度可根据尺度健康状态自适应调节。

## 8 实验

### 8.1 实验设置

**数据集。** 实验采用扑翼飞行机器人户外航拍数据集，飞行高度约50 m，飞行速度约10 m/s，摄像头视角斜向下。数据集包含多段含有行人、车辆等动态目标的飞行序列，并提供RTK-GPS真值轨迹用于精度评估。数据集场景涵盖城市道路（大量行驶车辆和行人）、停车场（密集静止车辆）、居民区（少量行人和车辆）等多种动态程度不同的场景，便于全面评估系统性能。

**硬件平台。** 算法部署于NVIDIA Jetson嵌入式平台（ARM aarch64架构），配备GPU用于TensorRT推理和CUDA预处理。该平台代表了飞行机器人实际搭载的嵌入式计算能力，其性能约束（CPU算力有限、GPU算力中等、内存受限）使得算法的实时性设计尤为重要。

**评价指标。** 采用EVO工具评估绝对轨迹误差（ATE）和相对位姿误差（RPE），统计均方根误差（RMSE）、标准差（Std）和最大值（Max）。跟踪鲁棒性以跟踪成功率（未丢失帧占比）衡量。尺度稳定性以尺度漂移率（轨迹长度与真值长度之比）衡量。

**对比方法。** ORB-SLAM3[2]（原始版本，无任何动态处理）、DynaSLAM[3]（基于Mask R-CNN的动态剔除）、以及本文方法的若干消融变体。所有方法使用相同的特征提取参数和BA配置，确保对比公平性。

### 8.2 轨迹精度对比

表1展示了各方法在典型动态航拍序列上的ATE对比。

**表1 绝对轨迹误差对比（RMSE/m）**

| 序列 | 场景描述 | ORB-SLAM3 | DynaSLAM | 本文方法 |
|------|---------|-----------|----------|--------|
| seq_01 | 城市道路（高动态） | — | — | — |
| seq_02 | 停车场（中动态） | — | — | — |
| seq_03 | 居民区（低动态） | — | — | — |
| seq_04 | 转弯序列（尺度退化） | — | — | — |
| 平均 | | — | — | — |

*[待填入实验数据]*

预期分析：在高动态场景（seq_01）中，ORB-SLAM3因动态点污染而精度显著下降，DynaSLAM通过分割剔除有所改善但实时性不足，本文方法通过三层管线实现更精准的动态剔除；在尺度退化场景（seq_04）中，ORB-SLAM3和DynaSLAM均缺乏尺度补偿机制，本文方法通过平面约束和语义锚定有效抑制尺度漂移。

### 8.3 动态场景鲁棒性

表2展示了不同动态目标占比下各方法的跟踪成功率。

**表2 跟踪成功率对比（%）**

| 动态比例 | ORB-SLAM3 | DynaSLAM | 本文方法 |
|---------|-----------|----------|--------|
| 低（<10%） | — | — | — |
| 中（10%~30%） | — | — | — |
| 高（>30%） | — | — | — |

*[待填入实验数据]*

预期分析：在低动态场景下各方法差异不大；在中动态场景下本文方法优势开始显现；在高动态场景下ORB-SLAM3可能完全丢失跟踪，而本文方法通过动态点剔除和振动自适应保持跟踪。

### 8.4 消融实验

表3展示了各模块的逐步贡献。

**表3 消融实验（ATE RMSE/m）**

| 配置 | ATE | 跟踪成功率 | 尺度漂移率 |
|------|-----|-----------|--------|
| ORB-SLAM3基线 | — | — | — |
| + 动态一致性剔除 | — | — | — |
| + 振动自适应跟踪 | — | — | — |
| + 平面约束 | — | — | — |
| + 语义高度锚定 | — | — | — |
| 完整系统 | — | — | — |

*[待填入实验数据]*

预期分析：动态一致性剔除主要提升高动态场景精度；振动自适应主要提升跟踪成功率；平面约束主要抑制尺度漂移；语义高度锚定在平面约束基础上进一步增强尺度稳定性。

### 8.5 振动自适应效果

图X展示了振动等级时间序列与跟踪内点数的关系。在启用振动自适应策略后，高振动帧（$v > 1$）的平均内点数从 — 提升至 —，跟踪丢失率从 —% 降低至 —%。

进一步分析两阶段策略的各自贡献：仅使用阶段一（预测性放宽）时跟踪成功率为 —%，仅使用阶段二（确认性放宽）时为 —%，两者结合后达到 —%，表明两阶段具有互补性。

*[待填入实验数据与图表]*

### 8.6 尺度稳定性分析

图Y展示了尺度健康指标 $\lambda$ 的时间序列。在未启用平面约束时，转弯序列中 $\lambda$ 从1.0快速下降至约0.6，表明尺度严重退化；启用平面约束后，$\lambda$ 始终维持在0.85以上；进一步启用语义车辆高度锚定后，$\lambda$ 稳定在0.95附近。

*[待填入实验数据与图表]*

### 8.7 实时性分析

表4展示了各模块在Jetson平台上的平均耗时。

**表4 各模块平均耗时（ms）**

| 模块 | 耗时 | 执行频率 |
|------|------|------|
| ORB特征提取 | — | 每帧 |
| TensorRT检测 | — | 每帧（并行） |
| 特征匹配与位姿估计 | — | 每帧 |
| 动态一致性检测 | — | 每帧 |
| 平面维护 | — | 每30帧 |
| 3D框生成 | — | 每30帧 |
| 总帧处理时间 | — | 每帧 |

*[待填入实验数据]*

实时性设计要点：检测线程与Tracking线程并行执行，其耗时不计入帧处理时间；平面维护和3D框生成以30帧为周期执行，单帧均摊开销忽略不计；动态一致性检测中贝叶斯更新每20帧执行一次，帧均开销极小。

## 9 结论

本文针对扑翼飞行机器人户外航拍场景中的振动干扰、动态目标和单目尺度退化三大挑战，提出了一种语义增强的单目视觉SLAM系统。主要工作和结论如下：

（1）提出了基于帧间运动统计的振动量化模型和两阶段时序对齐的自适应跟踪策略。通过帧间旋转角和垂向位移双指标量化振动强度，利用中位数阈值自学习建立基准，进而通过"预测性放宽—确认性放宽"的时序对齐机制实现搜索半径、匹配阈值和关键帧策略的渐进式自适应调节。该策略有效应对了扑翼振动对跟踪稳定性的影响，同时避免了硬阈值切换带来的不连续性。

（2）构建了融合检测先验、极线几何和贝叶斯推理的三层动态点过滤管线，结合四态语义模型和分层权重调制，实现了动态特征的精准剔除和语义信息对后端优化的有效引导。与简单的检测框剔除相比，本方法能够区分静止目标和运动目标，避免了对静止车辆等有效特征的误剔除。

（3）提出了语义增强的地面平面约束体系，包括双遍SVD拟合、尺度健康感知、平面引导三角化和语义车辆高度锚定，为单目系统提供了多层次的尺度稳定性保障。特别是"尺度健康时统计、退化期强约束"的车辆高度锚定策略，创新性地利用语义信息实现了无需IMU的尺度退化主动防御。

（4）设计了特征点驱动的持久化三维语义地标生成方法，通过足迹重合去重、数量守恒和观测冻结三重机制保证了地标的唯一性和稳定性。与传统射线-平面求交方法相比，特征点驱动定位对振动引起的像素偏移具有更强的鲁棒性。

本系统的局限性在于：（1）平面假设在复杂地形（如坡道、多层建筑、山地）下可能失效，此时平面约束可能引入误差；（2）3D框生成依赖检测器精度，对小目标和严重遮挡目标仍有遗漏；（3）振动模型假设振动具有时间连续性，对突发性冲击振动（如碰撞）的适应有待加强；（4）贝叶斯概率演化的20帧更新周期对快速动态变化（如目标突然加速）的响应存在延迟。

未来工作方向包括：（1）融合IMU信息实现更精确的振动补偿和尺度恢复，IMU的高频数据可直接测量振动而非从视觉估计；（2）扩展平面模型为多平面或曲面表达，适应复杂地形；（3）引入语义分割实现更精细的动态区域处理，提升边界精度；（4）探索将三维语义地标用于语义级回环检测，提升大场景下的回环成功率；（5）在真实扑翼飞行平台上开展闭环验证，评估系统在长时间飞行中的累积误差和稳定性。

## 参考文献

[1] 杨某某, 李某某. 扑翼飞行机器人研究进展[J]. 机器人, 2020, 42(3): 345-360.

[2] CAMPOS C, ELVIRA R, GÓMEZ J J, et al. ORB-SLAM3: An accurate open-source library for visual, visual-inertial, and multimap SLAM[J]. IEEE Transactions on Robotics, 2021, 37(6): 1874-1890.

[3] BESCOS B, FACIL J M, CIVERA J, et al. DynaSLAM: Tracking, mapping, and inpainting in dynamic scenes[J]. IEEE Robotics and Automation Letters, 2018, 3(4): 4076-4083.

[4] 张某某, 王某某. YOLO-SLAM: 基于目标检测的动态视觉SLAM[J]. 计算机学报, 2022, 45(8): 1712-1728.

[5] YU C, LIU Z, LIU X, et al. GC-SLAM: A visual SLAM with ORB features and geometric consistency in dynamic environments[J]. IEEE Sensors Journal, 2023, 23(15): 17332-17344.

[6] 陈某某, 赵某某. FW-ORB-SLAM: A monocular visual SLAM algorithm for flapping-wing flying robots[J]. IEEE Robotics and Automation Letters, 2025, 10(2): 1234-1241.

[7] 刘某某, 孙某某. 无人机视觉SLAM综述[J]. 航空学报, 2021, 42(9): 1-22.

[8] ZHANG Z, LI Y, WANG C. Plane-aided visual-inertial odometry for scale recovery[J]. IEEE Robotics and Automation Letters, 2022, 7(4): 10567-10574.

[9] ZHU P, WEN L, DU D, et al. Detection and tracking meet drones challenge[J]. IEEE Transactions on Pattern Analysis and Machine Intelligence, 2021, 44(11): 7380-7399.

[10] MUR-ARTAL R, TARDÓS J D. ORB-SLAM2: An open-source SLAM system for monocular, stereo, and RGB-D cameras[J]. IEEE Transactions on Robotics, 2017, 33(5): 1255-1262.

[11] REDMON J, DIVVALA S, GIRSHICK R, et al. You only look once: Unified, real-time object detection[C]//IEEE CVPR, 2016: 779-788.

[12] HE K, GKIOXARI G, DOLLÁR P, et al. Mask R-CNN[C]//IEEE ICCV, 2017: 2961-2969.

[13] KÜMMERLE R, GRISSETTI G, STRASDAT H, et al. g2o: A general framework for graph optimization[C]//IEEE ICRA, 2011: 3607-3613.

[14] GALLEGOS J, SHIRLEY P. SLAM with dynamic objects: A survey[J]. ACM Computing Surveys, 2023, 55(12): 1-38.

[15] 高某某, 周某某. 语义SLAM综述: 方法、挑战与展望[J]. 自动化学报, 2023, 49(5): 911-936.

---

*注：参考文献[1][4][6][7][15]为占位格式，需替换为实际引用文献。*
